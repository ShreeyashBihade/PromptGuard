# PromptGuard Core

Reusable prompt intelligence extracted from PromptGuard.

This package is intentionally UI-free and VS Code-free. It will become the shared analysis and optimization engine for the extension, CLI, proxy, and SDK layers.
